apiVersion: v1
kind: ServiceAccount
metadata:
  # NÃO renomear: a role do OpenBao (openbao/setup.sh) está amarrada a esta SA
  # neste namespace (prod-apps). Mudar aqui quebra a injeção de segredo.
  name: podinfo-sa
